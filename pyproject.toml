[project]
name = "approved-sql-rag-agent"
version = "0.1.0"
description = "RAG agent that retrieves and safely executes approved SQL"
requires-python = ">=3.11"
dependencies = [
  "anthropic>=0.40,<1",
  "asyncpg>=0.29,<1",
  "fastapi>=0.115,<1",
  "fastembed>=0.4,<1",
  "httpx>=0.27,<1",
  "pydantic-settings>=2.6,<3",
  "python-dotenv>=1,<2",
  "PyYAML>=6,<7",
  "sqlglot>=26,<29",
  "uvicorn[standard]>=0.32,<1",
  "workos>=10,<11",
]

[project.optional-dependencies]
dev = [
  "httpx2>=2.0,<3",
  "pytest>=9.0.3,<10",
  "pytest-asyncio>=1.2,<2",
  "pip-audit>=2.7,<3",
  "ruff>=0.8,<1",
]

[project.scripts]
sync-approved-queries = "app.sync:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["app"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
markers = [
  "integration: requires live catalog and security PostgreSQL services",
]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP"]
